numpy
trimesh
manifold3d
shapely
svgelements
opencv-python-headless
pillow
matplotlib

# --- selftest.py only; tilegen.py itself needs none of these ---

# trimesh loads a written 3MF back in section 5 to check it round-trips.
# That path needs its 3MF reader (lxml) and its scene-graph extras; trimesh
# stubs them out silently, so without these the section dies on import deep
# inside trimesh rather than reporting a missing dependency.
lxml
networkx
scipy

# Section 4's independent reference renderer. Needs the native cairo library
# as well:
#   macOS         brew install cairo
#   Debian/Ubuntu apt install libcairo2
# Without it that one section skips; everything else still runs.
cairosvg
